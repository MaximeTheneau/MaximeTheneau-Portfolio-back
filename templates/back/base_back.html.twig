{% extends "base.html.twig" %}

 {% block body %}

    <nav class="navbar navbar-expand-lg bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_back_categories_index') }}">Back-Office</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_back_categories_index') }}">Films et séries</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_back_user_index') }}">User</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="mb-4">
		<div class="container px-4 px-lg-6">
			<div class="row gx-4 gx-lg-5 justify-content-center">
				<div class="col-md-10 col-lg-8 col-xl-7">
					<div class="my-5">
    
                        {% block content %}{% endblock %}

                    </div>
				</div>
			</div>
		</div>
	</main>
 {% endblock %}