<form method="post" action="{{ path('app_back_subtopic_delete', {'id': subtopic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subtopic.id) }}">
    <button class="btn">Delete</button>
</form>
